import type { ThemeConfig } from "antd"
import { theme } from "antd"

const config: ThemeConfig = {
    algorithm: theme.defaultAlgorithm,
    token: {
        colorPrimary: "#D5001C",
        colorInfo: "#D5001C",
        colorTextSecondary: "#565A5D"
    }
}

export default config
